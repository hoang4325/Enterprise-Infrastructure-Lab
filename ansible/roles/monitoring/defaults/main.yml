# Thu muc chua Monitoring Stack
monitoring_directory: /opt/monitoring

# File Docker Compose
compose_file: docker-compose.yml

# File cau hinh Prometheus
prometheus_config: prometheus.yml

# Ten service Docker
docker_service: docker

# Cong Node Exporter tren cac may duoc Prometheus scrape
node_exporter_port: 9100
