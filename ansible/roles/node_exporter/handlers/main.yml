---
# Reload systemd và khởi động lại Node Exporter khi binary hoặc unit thay đổi.
- name: Restart Node Exporter
  ansible.builtin.systemd_service:
    name: node_exporter
    state: restarted
    enabled: true
    daemon_reload: true
