#File duoc tao tu template neu file thay doi cau hinh docker tu restart qua handler
- name: Configure Docker daemon
  template: 
    src: daemon.json.j2
    dest: /etc/docker/daemon.json
    owner: root
    group: root
    mode: "0644"
  notify: Restart Docker

